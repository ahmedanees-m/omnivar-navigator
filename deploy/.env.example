# Copy to .env (git-ignored) and fill in. Never commit real values.

# --- VM access (key-based auth preferred; password supported for convenience) ---
VM_HOST=10.30.158.35
VM_USER=anees_22phd0670
VM_PORT=22
# VM_KEY=/path/to/id_ed25519
# VM_PASSWORD=

# --- Cloud Nemotron (NVIDIA NIM, OpenAI-compatible) ---
NVIDIA_API_KEY=
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
# Optional: point the "fast" route at a local Ollama endpoint instead of cloud
# OLLAMA_URL=http://ollama:11434/v1

# --- Release automation only ---
# GITHUB_TOKEN=
